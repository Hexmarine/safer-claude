---
name: daily-email-volume-overdue-job-reminders
description: "Daily prod email volume + bounce health: ~600/day is 83% overdue-job reminders (stale pools closed 2026-06-20); the 8% bounce was SES SUPPRESSION-LIST re-bounces; SES capture pipeline + no-kill-switch facts"
metadata:
  node_type: memory
  type: project
  originSessionId: b38afd56-c669-467d-9776-233dc9ca5f6c
---

Verified 2026-06-20 via full dump of the SES capture queue (SNS→SQS `ses-events-sensorglobal-prod`, RAW message delivery — Body IS the SES event, no SNS wrapper) + MySQL `tbl_jobs`.

**Daily prod email = ~600–720/day (SES get-send-statistics), and it's NOT a storm — normal baseline.** Full queue dump (2,630 Send events): **83% (2,196) are OVERDUE-JOB REMINDERS**, 8% job assign/complete, 2% device alert, 1% data-import notice (a partial SQS peek over-weighted import to ~28% — SQS sampling is front-biased, only a full pass is trustworthy), rest install/test/flapping-pause. Recipients dominated by smoke-alarm CONTRACTOR domains (cwsainfo, n2electrical, syncom, taskforce, homesafetyballarat…), Haven hhs.org.au only ~136, internal sensorglobal.com ~83. ~134 bounces in window (~5%).

**Overdue-job reminder cron** (`check-overdue-jobs-cron`/`-to-agency-cron`; jobs.entity.ts ~7370+, `overdueNotificationSent` flag OVERDUE_JOB_NOTIFICATION SENT_TO_AGENT/SENT_TO_ALL). Overdue = `tbl_jobs.status=2 (JOB_STATUS.ACCEPTED) AND jobTime<now` (jobTime epoch-ms). `tbl_jobs` is MySQL with `agencyId` direct (Haven=37413).

**Overdue jobs = 1,357: Haven 224, non-Haven 1,133 (83%).** Age (by jobTime): 0-30d 163, 30-90d 618, 90-180d 304, 180-365d 272 (ALL 272 non-Haven), oldest 2025-07-14 (~11mo). Haven's own oldest 2026-01-16 (migration floor), none >180d, 167 in 90-180d. So the email volume is driven by STALE non-Haven (old-Sensor pre-JV) overdue jobs that will never complete but re-remind contractors daily.

**VERIFIED 2026-06-20 22:51 UTC (recheck via SES get-send-statistics):** the overdue-reminder cron fires as ONE batch at **21:00 UTC daily** (the day's dominant spike — compare batch-to-batch, NOT noisy day-totals). First batch after the close DROPPED **~547→395 (~28%, ~152 fewer emails)** vs the 06-18/06-19 batches (545/549). Day-total 06-20 ~430 vs ~600-720 baseline. NB the reminder is batched PER RECIPIENT/contractor, not per job, so 442 jobs closed ≠ 442 emails saved (~152 actual). Remaining 395 = still-live overdue pool (Haven 224 + non-Haven <90d). Confirms the 30-90d band (618 non-Haven) will age into the tail and re-inflate → durable lifecycle auto-close needed, not a one-time sweep.

**APPLIED 2026-06-20 (user "go - tbl_jobs", see docs/applied-changes.md):** closed **442** stale non-Haven overdue jobs — `UPDATE tbl_jobs SET status=9 (CLOSED) WHERE status NOT IN (5,9,3,0) AND jobTime<now-90d AND (agencyId<>37413 OR agencyId IS NULL)` (was 409 Accepted/21 Pending/12 EN-Sched; jobTime 2024-07-18→2026-03-21). Raw SQL in a txn, no hooks → no cancellation emails. Undo snapshot (id,original_status) at `ops-and-extracts/closed-nonhaven-overdue-jobs-2026-06-20.snapshot.csv`. The reminder cron `sendOverdueJobsEmail` (jobs.entity.ts ~8292) excludes CLOSED, so their reminders stop. RECHECK SES get-send-statistics 2026-06-21 to confirm the daily volume drop.

**APPLIED 2026-06-20 (second sweep, user "go - tbl_jobs"):** closed the **remaining 753** non-Haven overdue jobs (the full pool — all <90d after the 442 close; was 724 Accepted/28 Pending/1 NOE-Sched). Same txn pattern via `scripts/diag/sensor-jobs-close.js` on instance i-04e07d55ceed43790. Verified SNAPSHOT=753, AFFECTED=753, TARGET_REMAINING=0, Haven still 224 (untouched). Undo snapshot `ops-and-extracts/closed-nonhaven-overdue-jobs-2026-06-20b.snapshot.csv`. **Decision rationale:** per-agency check showed ALL 15 non-Haven agencies dormant — 0-13 jobs created in 90d, last job touched Mar-May (the "recent" jobTime due-dates were scheduled-out on abandoned jobs; updatedAt is the real activity signal, not jobTime). Predicate self-limits to past-due jobs so the 3 faintly-active agencies kept forward work. **Non-Haven overdue pool is now 0** (442+753 closed total); only Haven's 224 legit overdue remain. NEW diag tooling built: `scripts/diag/sensor-sql-read.js` (read-only, refuses non-SELECT) + `sensor-jobs-close.js` — both fetch DB creds the app's way (SECRET_NAME→Secrets Manager→Object.assign process.env; DB creds are NOT in the app .env nor /proc environ because dotenv/secret-assign happens at runtime), never print the secret.

**HAVEN's 224 overdue dissected 2026-06-20 (the legit in-scope pool):** ALL status=2 Accepted, 1 job per property (224 distinct props), bulk onboarding cohort, none touched in 30d. Age: 167 are 90-180d overdue, 53 are 30-90d, 4 fresh (bumped against Haven's ~Jan-16 migration floor = essentially the whole original intake unfinished). Concentrated on just **2 contractors**: (1) **Syncom** (tradePersonId 53410, syncom.com.au) = **189** — ACTIVE contractor (408 completed all-time, touched a job 2026-06-19) but huge unworked backlog; the ~189 ≈ the "~200 open Syncom jobs" from [[saferops-myjobs-page-dilution]] → strong hypothesis they CAN'T SEE most of their jobs in the portal (page-dilution bug), so the work stalls. (2) **Taskforce** (tradePersonId 11125, taskforce.com.au) = **35** — DORMANT (0 started, no activity since 2025-12-18, all 35 due same day ~Jan-16, abandoned batch). **APPLIED 2026-06-20 (user "go - tbl_jobs"): closed Taskforce's 35** (status 2→9) via `scripts/diag/sensor-jobs-close-taskforce.js`, verified AFFECTED=35/REMAINING=0/Syncom still 189. Undo `ops-and-extracts/closed-haven-taskforce-jobs-2026-06-20.snapshot.csv`. **Haven overdue now = 189, all Syncom** — NOT closed (real work); lever = verify/fix portal page-dilution so Syncom can see+burn them down, then chase. Open next steps: (a) confirm page-dilution fix is in prod, (b) decide whether to reassign Syncom's backlog.

## Merged from ses-sns-health-and-bounce-source (2026-06-16→18 investigation; full trail archived to docs/investigations/2026-06-16-ses-bounce-source-and-comms-health.md)

- **Comms health verified 2026-06-16:** SES healthy/sending (domain sensorglobal.com DKIM-verified); SNS SMS works end-to-end, dormant only via the SEND_SMS gate ([[sensor-sms-gating-and-failure-report]]).
- **The 8% bounce was SES ACCOUNT SUPPRESSION-LIST re-bounces, not dead addresses:** every captured bounce was `Permanent/OnAccountSuppressionList` — SES refuses delivery to suppressed addresses and counts each retry as a fresh bounce (self-perpetuating). **Structural bug still open: `MailManager` never checks the suppression list before sending**, so any hard-bounced address re-bounces forever (Category B/C code fix). Valid mailboxes need `sesv2 delete-suppressed-destination` after correcting the address.
- **Still open:** contractor John Trotter (id 37383, `sensor@homesafetyballarat.com`, suppressed 2026-05-18) has ~80 REAL jobs — needs a corrected email from the agencies + suppression removal, NOT job closure.
- **SES observability pipeline (the durable fix, LIVE):** config set `sensorglobal-prod-events` → SNS → SQS `ses-events-sensorglobal-prod` (RAW delivery — Body IS the SES event). Before it, bounced addresses were unrecoverable (no DB persists sends). SQS peeks are front-biased — only a full queue pass is trustworthy.
- **No global email kill-switch exists** (no SEND_EMAIL flag; only SEND_SMS). Gating is the wrong tool anyway — capture + address hygiene is. Recurring comms are cron-PULLED from the cron box (crons = plain `curl` with a plaintext Basic-auth header — security gap, docs/gaps.md item 5).
- **Applied 2026-06-17/18** (docs/applied-changes.md): closed 27 internal/test overdue jobs; paused Victoria Munro's broken partial PropertyTree feed (`tbl_import_source` id167 → 0 — feed returned 2/20 props → 18 S114 warnings/day + standing deactivation exposure); killed the ConsoleCloud ZOMBIE (deactivated agency 37567 still syncing 16×/day for ~11mo — token id5 deleted; **ConsoleCloud is webhook/token-driven, NOT gated by tbl_import_source**); deleted 2 dormant orphan CC tokens (id3/id4).
- PropertyMe dead-since-04-09 root cause and the unguarded daily full-snapshot reconcile risk: see [[pms-integrations-propertyme-propertytree-status]] and [[haven-mass-deactivation-2026-06]].
- Stale INVITED properties (1,289, status=5) are NOT a noise source — `invitationReminder` fires only 1/2/4/5 wks post-invite then stops (docs/gaps.md item 8; no mutation warranted).
- Property status enum (app.ts PROPERTY_STATUS): 0=INACTIVE 1=ACTIVE 2=DELETED 4=NEW 5=INVITED 8=ACCEPTED 10=DISCONNECTED 11=DEACTIVATED.

**DURABLE FIX DIRECTION (user decision 2026-06-20): NOT agency-scoping.** Do NOT scope the overdue-reminder cron to Haven/an agency allowlist — there will be other NEW non-Haven JV clients who SHOULD get reminders, and an allowlist would wrongly mute them. The right lever is **staleness/lifecycle-based** (applies to ALL clients equally): e.g. auto-CLOSE jobs once overdue beyond a threshold, and/or decay/stop the reminder cadence after N days overdue (so a job reminds for a while then stops, instead of forever). That kills stale-job noise for any client without excluding any client. Still pending: the 565 non-Haven jobs in the 30-90d band (will age past 90d; could close with a 30d cutoff), and Haven's own 224 overdue (legit, in-scope — for Haven to action). Related: [[ses-454-flapping-crash-email-storm]] (the "storm" was baked-AMI history, separate), [[sensor-outgoing-comms-map]].
