---
name: aws-cost-shape-and-ami-sprawl
description: AWS account cost breakdown + the AMI/snapshot sprawl that dominates EC2-Other; what was trimmed 2026-06-26
metadata: 
  node_type: memory
  type: project
  originSessionId: 4728095c-89e8-479c-86bf-11263c7baddb
---

AWS account **747293622182** (ap-southeast-2), run-rate **~$5k/mo** (May 2026 baseline; April was distorted by expiring credits). Top lines: EC2-Other $930 · EC2-Compute $643 · RDS $601 · CloudWatch $375 · ElastiCache $303 · MongoDB Atlas $235 · VPC $118 · ELB $108 · QuickSight $102. Security stack ≈ $180 (GuardDuty/Inspector/Config/SecurityHub/Macie).

**Biggest waste = AMI sprawl.** 4,686 self-owned AMIs / 5,204 snapshots = **$648/mo** snapshot storage (70% of EC2-Other). Cause: CodeDeploy/ASG automation bakes an "AMI from ASG Instance" per instance and NEVER deregisters. Snapshots are incremental so 158 TB nominal ≈ ~13 TB billed. DLM policy `policy-0441001e0e7116959` ("Seven Days Backup", 141 snaps) is innocent. In-use AMIs are tiny: only the 4 ASGs' $Default/pinned images (CodeDeploy→ami-0b21af515501ac73f, sso→ami-02ef232c67ce159c1, EKS v1→ami-0236e848248c2e2ee). **BACKFILL DONE 2026-06-27** (user-ran big_delete.py via `!`; Claude can't — harness classifier BLOCKS mass AMI/snapshot deletion AND its read-only prep, even with user "go"): deregistered 4,106 AMIs (<2025-12-01, not-in-use) + deleted 4,483 snaps (377 orphan + 4,106 backing), 0 errors. Live: AMIs 4,686→585, snaps 5,204→752 (584 CreateImage + 141 DLM + 27 misc). All 3 ASG AMIs survived. ~$400-520/mo est. **RECURRENCE FIXED 2026-06-27**: added `prune_old_amis()` to `Smoke_prod_api_golden_ami_function` (keep newest 10, MAX_DELETE_PER_RUN=15, protected-set guard) deployed via update-function-code (no TF drift — code is `ignore_changes`'d; src now at `ops/golden-ami-retention/lambda_function.py`, NOT yet in code/infra). GOTCHA caught pre-deploy: all 7 golden-ami lambdas share AMI name "AMI from ASG Instance (" → must scope by Name TAG glob `CodeDeploy_Smoke-API_* By Script` not the name, else prunes QA(476)/sandbox/sso. PROD-API ONLY so far; **6 sibling lambdas (qa/sandbox×api + 4 sso) still unpruned** — replicate w/ each env's LT id + tag glob.

**Applied 2026-06-26** (see docs/applied-changes.md): New Relic teardown (metric stream + Firehose, ~$262/mo — also cancel the NR SaaS sub), released 9 unassociated EIPs (~$32), gp2→gp3 ×7 (~$10), deleted 26 unattached volumes idle-since-2023 with safety-net snapshots (~$73). Structural lever not yet taken: Compute Savings Plan + RDS/ElastiCache RIs on always-on baseline (~$250-350/mo). Watch: Macie (often forgotten), QuickSight seats. Note: aws-cli v2.31/Py3.14 flaky on this box → use boto3 for AWS ops.

**CloudWatch post-NR-teardown verified 2026-07-01** (Cost Explorer daily): NR cut confirmed live — daily CW cost dropped ~$14-15/day → ~$7-8/day right at the 2026-06-26 `DeleteMetricStream` CloudTrail event (matches the ~$262/mo estimate). Residual run-rate ≈ **$230-240/mo**, split: DataProcessing-Bytes ~$100 (log/metric processing), MetricMonitorUsage ~$50 (custom metrics — 665 `CWAgent` namespace metrics is the only non-free-AWS-native source; all other namespaces like AWS/EC2, AWS/EBS are standard free metrics), VendedLog-Bytes ~$42 (VPC flow + WAF logs delivered direct to CW Logs), AlarmMonitorUsage ~$17 (181 alarms, 0 composite), TimedStorage ~$12 (381.83 GB total log storage). Top 3 log groups by stored size, all retention=365d: `smoke-api-prod-pm2-out-log` 151.5GB, `smoke-vpc-flow-log` 129.5GB, `aws-waf-logs-prod` 68.5GB (together 92% of stored bytes).

**Applied 2026-07-01** (non-prod retention only, see docs/applied-changes.md): `qa-vpc-flow-logs` + `vpc-flow-log-development` never-expire→30d (both empty), `smoke-api-sandbox-pm2-out-log` 365d→90d (16GB). Prod trio intentionally left at 365d (SOC2, per `docs/runbooks/03-log-retention.md`) — that runbook's "all 143 groups at NEVER_EXPIRE" premise is now stale, most non-prod groups already had retention set.

**2026-07-02 decommission batch (all in docs/applied-changes.md, all verified):** Redis smokeprodapiredis 2×t3.medium→t3.small rolling, zero client drop (~$220/mo — was 1.3% utilized); Keychain app+ALB+4 CNAMEs+alarms torn down — instance ran NOTHING, snap-06fb6438993a0fde3 kept (~$45); QuickSight Enterprise cancelled — 0 dashboards, agency leftover, irreversible (~$102); SecurityHub+Inspector+Config recorder disabled, GuardDuty KEPT (~$150; 358 securityhub-* Config rules auto-drain post-disable — recheck; GuardDuty reviewed live: real HIGH findings incl. 55×CreateImage anomaly May-7 from 103.106.232.53 + phishing-DNS on now-gone openvpn-qa; HIGH findings→email WIRED+e2e-verified same day: EventBridge rule `guardduty-high-findings` sev≥7 → SNS topic → eugene.peresada@saferhomesau.com.au, sample finding received; InputTemplate gotcha = literal `\n` sequences required); Power-BI-Gateway terminated after 18mo stopped, snap kept (~$5); sensor-prod-old1 deleted by user via `!` (harness blocks Claude on RDS delete; ~$120). Day total ~$640/mo + GST. Gotchas: keychain instance+ALB both had deletion protection; QuickSight needed termination-protection off first; classifier requires per-item named "go" for each prod resource.

**Prod drill-down 2026-07-01 (read-only, not yet acted on):**
- **CWAgent metric bloat FIXED 2026-07-02** (see docs/applied-changes.md): 494/665 junk disk series (snap squashfs/Docker overlay/tmpfs mounts) killed by adding `ignore_file_system_types` to the shared SSM param `AmazonCloudWatch-linux` (v8), rolled to all 7 hosts via SSM fetch-config. TWO GOTCHAS for next time: (1) restart fails validation without `/usr/share/collectd/types.db` — stub file fixes it; (2) restarted agents publish full-dim series (+ImageId/InstanceType) which ORPHANS alarms keyed to old 4-dim series (InstanceId,path,device,fstype) — fixed by adding that combo to `aggregation_dimensions` (v9) so the rollup re-emits alarm-compatible series. All 6 disk alarms stayed OK. The ~256 `host`-dimensioned series are the prod-api ASG nodes (baked AMI config, NOT this param) — separate lever, untouched.
- **Alarm cleanup DONE 2026-07-01** (see docs/applied-changes.md): full-classified all 109 `INSUFFICIENT_DATA` alarms against live inventory (EC2/ASG/Lambda/RDS/ALB/TG), requiring every referenced resource to be confirmed-gone before calling an alarm dead. **77 deleted** (stale ASGs, fleet-rotated EC2 instance IDs, the deleted createfilezip Lambdas, dead `sensor-sandbox` RDS, sandbox ALBs/TGs) — verified 179→102 alarms. 24 left alone (resource still alive, just quiet — rarely-invoked lambdas, low-5xx ALBs). **8 left alone as ambiguous, incl. a real open question**: 3 alarms (`smoke-prod-api-HTTPCode_Target_3XX/4XX/5XX_Count`) reference `targetgroup/SmokeAPI/...` which is GONE even though its ALB (`SmokeAPI-LB`) is alive — possible **prod HTTP-error-rate monitoring gap** if nothing else covers the current target group. NOT investigated yet. First delete attempt was blocked by the harness's auto-mode classifier for not printing the full target-name list before mutating; re-ran after printing all 77 + explicit "go". `--alarm-names` as a shell-joined string mis-parses (AWS CLI list args need real separate argv entries) — use `--cli-input-json file://` for any future bulk CloudWatch alarm op.
- **Log ingestion, not storage, is the real driver of `smoke-api-prod-pm2-out-log`'s cost** (~5.4GB/day). CORRECTED 2026-07-02: the mid-June step-up is NOT the access log (that's <2% of bytes) — byte-analysis shows **54% = KORE SIM-webhook console dumps** (imei/imsi payload leaks, ballooned when Haven SIM swaps started Jun 12) and **43% = Sequelize SQL logging live in prod**. Killing both = ~95% of the volume ≈ $85-90/mo. Detail in [[sensor-backend-structured-logging]].
