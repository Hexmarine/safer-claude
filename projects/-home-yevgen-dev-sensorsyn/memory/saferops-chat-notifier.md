---
name: saferops-chat-notifier
description: "Google Chat notifier in safer-ops-api — alarm-event + backend-health notifications to ALERTS, morning status digest to DIGESTS; built+codex-clean 2026-06-13; committed since (worktree clean 2026-07-03); CI deploys on push — live state not separately verified"
metadata: 
  node_type: memory
  type: project
  originSessionId: 686f63b2-db38-4eaf-bca4-c3eb1d5c12ae
---

Google Chat monitoring notifier for Haven (agency 37413) in safer-ops-api. **Scope pivoted 2026-06-13: installation/swap progress dropped; now alarm-event + Sensor backend-health notifications.** Two spaces via incoming webhooks (ALERTS = real-time transitions, DIGESTS = scheduled), internal-only audience (full serials/addresses), headless auth = [[haven-ops-login-59256]] creds via sensorPasswordLogin, feature-flagged NOTIFIER_ENABLED. Plan file: ~/.claude/plans/nested-snacking-pie.md.

Built, all tested (318 green), codex-reviewed (one P2 fixed — see below). Committed since (worktree clean, verified 2026-07-03). Files in apps/api/src: chat-webhook.ts (FIFO/pacing/retry/3800-cap poster), notifier-auth.ts (token provider), notifier-state.ts (store + in-memory fake), notifier-transitions.ts (pure engine), notifier-probes.ts, notifier-digests.ts, notifier.ts (60s tick loop). 3 Prisma tables: notifier_incidents (dedupe), notifier_digest_runs (claim guard), notifier_state (KV). Migration 20260612203838_notifier_tables. **swap_baseline_devices + notifier-baseline-routes.ts were removed in the pivot.**

**Why:** push agency monitoring visibility; the live monitor (getMonitorSnapshot) is pull-only. Reuse its `agency:37413` cache key — NO new Sensor poll loop (WAF 500/5min took the monitor down 2026-06-11).

**How it works:**
- ALERTS stream: observeAlarmConditions reads the active-alert feed (full-fleet, not the 600-cap device grid). ALERT/TAMPERED post immediately; DISCONNECT/LOW_BATTERY debounce 30m (NOTIFIER_OFFLINE_THRESHOLD_MS — hubs flap on reboot/SIM swap); monitor:degraded after 3 consecutive ticks. planTransitions handles clear (only on non-degraded snapshot), flap suppression (15m cooldown), first-run baseline seed (health.bootstrapped marker → current conditions suppressed, one summary line). At-least-once: notifiedAt set only after poster.post() true.
- flapReopenIncident (user-authored): reset semantics — notifiedAt=null + firstSeenAt=now, keeps ...previous (suppressed, details/serials). Silent this tick; persisting issue resurfaces after debounce.
- Probes stream (notifier-probes.ts): sensor-api health-check (api_key, skipped if no key), sso-discovery (200 + issuer===localhost:4100), mqtt (isConnected via injected mqttTrace) + mqtt-stalled (latestSeq unchanged 15m). Fail conditions → probe:<name> consecutive-policy (3 ticks) through the SAME planTransitions. NOT baseline-suppressed (backend down at startup should page). Run before alarm stream so Sensor-down lands even during monitor backoff.
- **P2 fix (codex):** skipped probe (key removed/mqtt off) must NOT clear a prior failure — planTransitions takes `nonAuthoritativeKeys` set; runProbeStream passes skipped-probe keys so absence ≠ recovery.
- Morning digest (DIGESTS, claimDigestRun "morning-status" at NOTIFIER_DIGEST_TIME 08:00 Melbourne, threaded): backend section + active alarms from OPEN incidents (no extra probing), fleet counts from buildMonitorResponse summary (best-effort — posts without them on Sensor outage).

**Deploy:** create 2 spaces+webhooks → merge NOTIFIER_* into safer-ops/prod/app secret ([[saferops-prod-secret-sync]]: NOTIFIER_ENABLED=true, both webhook URLs, ops creds, NOTIFIER_HEALTH_API_KEY) → annotate externalsecret + rollout restart (migration runs via initContainer). notifierEffective() = flag + either webhook + ops creds + OIDC client config. Do NOT scale API >1 replica (digest claim guard only covers rolling overlap). Deployed 2026-06-13; backend probes verified green.

**Headless agency auth — THE HARD PART (resolved 2026-06-14, deploy pending):** `/users/alarms/list` needs a session-backed agency token. Sensor's `getTokenViaPassword` returns a session-less token → **423** loop. The ONLY accepted token = minted for OIDC client `safer_ops_client` + run through `/users/sso/exchange` (the operator flow). Headless = **ROPC against our SSO provider → exchange**, NOT getTokenViaPassword. notifier-auth.ts now: `passwordGrantWithCredentials(email,pw)` (oidc.ts, needs `compareType:"auth"`) → `exchangeOidcForSensorToken(idToken)` (account-scoped `…ByAccount` + NOTIFIER_SENSOR_ACCOUNT_ID if exchange returns multipleAccounts). Three prerequisites discovered the hard way:
1. The ops account ([[haven-ops-login-59256]]) is **userType AGENCY(2)**, not sub-admin.
2. **Enabled `password` grant on `safer_ops_client`** in sso-provider Mongo: `db.clients.updateOne({client_id:"safer_ops_client"},{$addToSet:{grant_types:"password"}})` (was authz_code+refresh only → `unauthorized_client`). DONE. May need provider rollout restart if client cached.
3. **sso-provider `src/actions/grants/password.ts` `setValuesInToken` must emit `"email_verified":"yes"`** (was emitting email without it). The exchange's trust anchor is `isEmailVerifiedClaim(decoded.email_verified)` + `aud===safer_ops_client`; without email_verified → "Invalid credential." One additive line, regression-guard SAFE/Class B. **Deploy sso-provider FIRST, then safer-ops.** NOTE: sso-provider worktree had unrelated secret-extraction WIP (cookie/JWK keys → env) — stashed; commit ONLY password.ts (user pop the stash after). The earlier shipped `userType:2` on sensorPasswordLogin (d1d1e82) is now superseded/reverted.

**DEPLOYED + VERIFIED 2026-06-14:** sso-provider deploys via **AWS CodePipeline `sso-api`** (NOT Flux — source=GitHub→build→CodeDeploy/ASG), logs in CloudWatch `sso-prod-api-logs`/`-error`. The 2026-05-31 E11000 grantId trap did NOT recur (grantId now non-unique in BaseModel.ts). email_verified fix confirmed: exchange "Invalid credential"→Success. Full chain ROPC→exchange→session-token→200 PROVEN.

**✅ FULLY LIVE 2026-06-14:** safer-ops notifier deployed (Flux, image main-20260614041820-0d8a591). In-cluster verified: ROPC→exchange auth succeeds, 102 sensor.request all status:200 (full Haven fleet read, alarms+controller+alerts paginated), baseline.seeded, baseline message posted to ALERTS Chat space, all 4 probes green, 6 clean ticks, zero 423/errors. The whole notifier feature is operational. Remaining optional: sensor-alarm-backend logging change (throttled WARN on AdminUserAuth 423 paths) ready to commit/deploy.

**THE `access_token` HEADER GOTCHA (cost hours):** Sensor's `/users/*` `AdminUserAuth` (sensor-alarm-backend src/middlewares/auth.ts) reads the bearer from the **`access_token` header** (format `bearer <tok>`), NOT `authorization`. Missing it → silent **423** (TOKEN_EXPIRED_423) BEFORE the token is even checked, with NO log. safer-ops SensorClient already sends `access_token: bearer ${token}` (sensor-client.ts:1215) — correct. Manual de-risk curls MUST use `-H "access_token: bearer $TOK"` not `authorization`. **TODO Sensor-backend logging fix:** add a log line on AdminUserAuth's silent 423 branches (no-header + session-validation reject) — would've made this instant.

**Flap dampening + fleet hysteresis (built 2026-06-20; committed since, verified 2026-07-03):** a faulty water-leak sensor toggling every ~16–200s spammed ALERTS with New-alert/Recovered pairs every tick (confirmed real device flap — see [[device-mqtt-event-history-tbl-alarm-logs]]; NOT a cap/artifact bug). Added to notifier-transitions.ts: per-incident edge history on `details` (flapTransitions[]/flapping/flapNotifiedAt/flapLastEdgeAt/flapLastState), `recordEdge` (prune 24h + cap 20), `isFlapping` (user-authored: ≥flap1hLimit/hr OR ≥flap24hLimit/24h, mirrors backend FLAPPING_ALERT_CONFIG 3/5), and a sticky state machine in planTransitions — once flapping, the incident is HELD OPEN across momentary clears (never set clearedAt mid-episode, or the clear loop's `clearedAt` skip strands it), suppressing BOTH reopens and the ✅ Recovered churn; ONE muted "⚠️ Flapping (muted)" notice (own buildAlertsMessage section) on entry; exits only after stable in one state for flapStableDwellMs (60m) → one resolution. flapNotifiedAt at-least-once in advanceTerminalState. Non-flapping (1–2 toggles) path UNCHANGED (flap branches gated on flapping===true). Config: NOTIFIER_FLAP_1H_LIMIT=3/24H_LIMIT=5/STABLE_DWELL_MS=3.6e6/HISTORY_CAP=20. SEPARATE fix: fleet-health ±11 oscillation (offline count 749↔760, threshold 10) → `fleetMoveConfirmed` hysteresis (NOTIFIER_FLEET_DWELL_TICKS=2, FLEET_PENDING_KEY state) — a move must hold N consecutive ticks; oscillation flips sign each tick → never confirms. All 369 api tests green; codex review could NOT run on this EC2 box (its bwrap sandbox: `loopback Failed RTM_NEWADDR` + harness blocks --dangerously-bypass) → run codex in CI/elsewhere. Field action still needed: replace the faulty Werribee sensor 26205. Plan: ~/.claude/plans/nested-snacking-pie.md.

**Health-probe 403 fix:** the `api_key` health endpoint 403 = IP not whitelisted (overloaded: also key-missing/inactive). Keys are owned (createdBy) so list looked empty; minted a fresh key + whitelisted the **EKS NAT egress IP `13.210.248.111`** (cluster vpc-03ec09702866ca7b1, single NAT; node in private subnet → that's the egress Sensor sees via x-forwarded-for). Probe → 200. Keys have a TTL (`expiryDate`/EXPIRED status) — set no/long expiry.
