---
name: clients-refactor-and-local-surface
description: "State of the clients/email-registry refactor — local prod-data surface, iteration loop, §1 done, extraction-quality backlog"
metadata: 
  node_type: memory
  type: project
  originSessionId: 59277a1d-4a4d-4987-8a7c-9e2520e40c79
---

Ongoing refactor of the email→client-registry pipeline (branch `feat/clients-refactring`), as of 2026-05-29. Uncommitted in the working tree.

**Local testing surface = REAL prod data.** The local dev DB (`email_ops_summary`) now holds a 90-day prod snapshot: 28 providers / 865 clients + baseline accepted events, and 2 Gmail accounts / 2129 threads / 3623 messages. Built with new tooling: `snapshot-inbox-export/apply.ts` (raw threads, OAuth tokens stripped) + existing `registry:baseline-export/apply` (clients, preserves prod IDs). Dumps live in gitignored `tmp/` and contain PII — never commit them. Tests run against a separate `email_ops_summary_test` DB (force `MODEL_PROVIDER=mock` since the shell exports gemini).

**Iteration loop** (for tuning extraction): `npm run roster:reset-suggestions` (wipes events/reviews/proposals globally, keeps clients) → `roster:reprocess -- --limit N` (real Gemini) → `roster:export-eval` → multi-agent eval workflow. The eval export MUST give agents the proposal's `rawInputText` (exact text the extractor saw) — feeding re-truncated source produces FALSE "hallucination" findings (learned the hard way).

**§2 done (collapse double extraction):** the auto pipeline now does ONE thread-level, multi-client, client-tagged rich extraction (`extractThreadRegistryItems` + `extract-thread-registry-items.txt`; `clientName` flows through the existing registry-provider plumbing — types, Gemini response+parse schemas, parser). Items are routed per client via `matchClientName` and drafted from the items (`createGmailProposalFromExtractedItems`, no re-extraction); `ClientEmailEvent` stays evidence-only (projected from the rich ops). Auto path went from 1+N Gemini calls → 1. Deleted the lean `extractClientEvents` + its prompts/test. FOLLOW-UP (trivial): the now-dead `LlmProvider.extractClientEvents`/`condenseForExtraction` methods + types remain (harmless). Validated: proposals correctly client-routed, invariants clean, Rick's SS booking now captured as schedule_change. The manual "draft from thread" `createGmailClientRegistryProposal` is unchanged.

**§1 done (implemented, verified, 431 tests green):** collapsed the two parallel apply systems — deleted the orphaned `RosterReviews*` UI cluster, `roster-review-service.ts`, the `ClientEmailEvent` apply path (`applyClientEvent`/`bulkApplyEvents`) and dead routes/types. `ClientEmailEvent` is now evidence-only; all mutation goes through proposals. See [[two-apply-systems-clientemailevent-vs-proposals]].

**Extraction quality (28-set grounded eval):** 7 good / 18 mixed / 3 poor, and crucially ZERO hallucinated entities — clients/workers/dates are real. FIXED so far: (1) `startTime` lifted from email Sent-timestamp → `stripEmailMetadataLines` in worker-time.ts (verified 0 leaks on 100 threads); (2) `dayOfWeek` vs eventDate → `enforceDayOfWeekMatchesDate` (date authoritative); (3) deterministic-fallback header-fact blowup → guardrail now on fallback path too; (4) PH-keep mislabeled `new_service` → new `public_holiday_service` eventType (prompt + deterministic re-map + display). Also FIXED: (6) PH-keep emitted directly as `new_service` by the LLM (re-map now also triggers on new_service) + (7) operational events whose note is just an email artifact (`[cid:…]`, footer link, header) → `isEmailArtifactNote` guard in `isLowValueOperationalNoise` (real-data items 64→44, ~20 junk events removed). Note: whether a keep-PH thread becomes `public_holiday_service` depends on the LLM's note carrying the keep signal (variance on ambiguous cancel↔keep reversal threads); the firm guarantee is it is never `new_service`. (5) reschedule "move to unknown day" was putting the ORIGINAL date in `eventDate` (the new-date slot) with `dateConfidence: exact` → `normalizeRescheduleUnknownTarget` parks the known date in `replacementDate`, nulls the (unknown) target, drops confidence. NOTE the canonical convention is `eventDate`=NEW/target, `replacementDate`=ORIGINAL/from (prompt lines 193-194) — the eval's "inversion" framing used the wrong convention. RESOLVED as NOT-a-bug: invented `recurrenceType` — verified 10/10 cases inherit the client's existing schedule cadence (legitimate operational continuity; eval flagged it only seeing the email). Also FIXED (eventType re-maps): (8) PH-keep emitted as `extra_service` now re-mapped too; (9) an enumerated recurring booking (≥3 same-time/worker dated `new_service`/`extra_service` points with ≥70% clean 7/14/28-day spacing) collapses to ONE standing `schedule_change` via `collapseEnumeratedRecurringServices` — fixes the "12 new_service cards" booking-confirmation case (Emilie Tabet). Also FIXED (significant): (10) the deterministic-fallback path in `client-registry-llm-extractor.ts` returned operational events WITHOUT normalization — so when Gemini returns invalid JSON and falls back (e.g. Rick Spielrein's long forwarded thread), every normalization/guardrail (dayOfWeek, startTime, eventType re-maps, collapse, artifact-drop) was bypassed. Now fallback events go through the same `normalizeAndFilterOperationalEvents` as the LLM path. NOTE: the LLM prompt change for PH-keep DID work (Gemini emitted `public_holiday_service`); the failure was invalid-JSON→fallback, not the prompt. OPEN robustness item: Gemini occasionally returns invalid/truncated JSON on long forwarded threads → fallback (no JSON-repair retry yet). RESOLVED as NOT-worth-building: no-empty-on-signal recall guard — only ~1/100 genuine silent miss (a dropped hold), ~80% false positives (tender/BD emails); real recall gaps are partial/secondary-signal → prompt-level. STILL OPEN (prompt/fixture work, lower-stakes or fuzzy): remaining wrong `eventType` (replacement-after-cancel→reschedule, retrospective-shortfall→billing_exception — not present in recent runs due to LLM variance), miscalibrated confidence on hearsay/enquiry (UX-only stakes since bulk-apply removed), partial-recall secondary signals. The deterministic fixes are largely done; what remains is prompt/fixture-driven. Prod migrations are separate — see [[prod-migrations-separate-from-app-deploy]].

**§3 done + Phases C–F done (as of 2026-05-30).** Phase F dropped vestigial `ClientEmailEvent.appliedToRecord` + `dismissed` (schema/repo writer/`api-roster.ts` type/`roster-client-detail-service.ts` + migration `20260530020000_drop_client_email_event_apply_fields`, hand-authored because `migrate dev` refuses non-interactive data-loss; applied via `migrate deploy`). Remember `prisma:gcp_deploy` for prod. Also done this session: the UI work — new `client/src/components/SearchableSelect.tsx` dark combobox replacing the white native datalist/select for the Identify manual-client fallback + provider picker (styled in `analysis-suggestions.css` with real theme vars), dropped the "→ suggested" line, removed top-right `ThemeToggle` from AppLayout (kept in Settings), renamed brand "Email Ops Summary"→"Ops Portal" (AppLayout + LoginPage).

**Extraction fixes (11 & 12) — verified on a fresh 200-thread eval (54 sets / 90 items):** (11) broadened PH-keep in `client-registry-normalization/event-rules.ts` — `saysKeepService` now also matches "go ahead"/"proceed"/"would like…service"/"require(s) service", `hasPublicHolidayContext` treats an existing `public_holiday_cancel` as inherent PH (so a keep stated only in the subject still flips), with a `saysCancelService` veto so genuine cancellations aren't flipped. Re-audit: 3 `public_holiday_service` (Dorothy/Rick/Lina, incl. the two previously-missed) + 7 `public_holiday_cancel` all genuine — zero false flips. (12) generic role descriptors ("her regular cleaner"/"today's cleaner"/"the support worker"/"regular carer") now → null in `normalizeWorkerName` (`worker-time.ts`); re-audit shows 0 leaks. Tests added in `client-registry-grounding-fixes.test.ts` + `client-registry-normalizer-schedule-time.test.ts`. NOT auto-changed (too ambiguous to generalize): PH cancel+reschedule/supersede pairs (e.g. Teresa idx 34/35, Natalina idx 36) and cross-thread duplicate bookings.

**Extraction fix (13) — reschedule supersedes a sibling cancel.** New `removeRescheduleSupersededCancellations` in `client-registry-llm-postprocess.ts` (wired into the normalizer chain after `removeRedundantReplacementServiceEvents`): when a `reschedule` carries both `eventDate` (new) and `replacementDate` (from), a standalone `cancel_once`/`public_holiday_cancel` on that same from-date is dropped as a redundant double-count. Guards: an independent cancel (no matching move) survives, and a `chargePolicy:"late_fee"` cancel survives (a `public_holiday_cancel`'s auto `chargePolicy:"public_holiday"` is just the PH marker, NOT a distinct billing event, so it does not block supersession). Verified on a fresh 200-thread eval: VIOLATIONS=0, the Beryle Tidey "PH 8/6 cancelled → moved to 10/6" double now collapses to one reschedule, 9 reschedules / 11 standalone cancels still healthy (no over-collapse). Tests in `client-registry-normalizer-cancellations.test.ts` (3 new: end-to-end collapse + 2 rule-isolation guards calling `removeRescheduleSupersededCancellations` directly — needed because a downstream recall guard drops a lone low-signal cancel at the `normalizeAndFilterOperationalEvents` level, masking the rule). NOT auto-changed (still too ambiguous): multi-`sw_no_show` across weeks, reschedule+reinstate back-and-forth, cross-thread duplicate bookings (different proposal sets — needs cross-thread state).

**Bug fix — accepting one suggested update made the whole card vanish.** Root cause: `applyClientRegistryProposal` flips a set to `partially_applied` once any item is applied (apply-service ~line 317), but three read paths filtered proposal sets on `status: "draft"` only, so a set with remaining draft items disappeared after the first accept. Fixed all three to `status: { in: ["draft","partially_applied"] }` (the `items.some({draft|edited})` clause already prevents fully-applied sets leaking back): review queue `client-registry-proposal-query-service.ts` (only when requested status is "draft"), the Suggested-updates badge count `analysis-identify-service.ts getAnalysisCounts` (`proposalSet.status` on the item-count where), and the provider summary `roster-service.ts pendingProposalWhere`. Left `registry-health-service.ts` draftSets/draftItems as-is (intentional draft-only diagnostics). Verified live-DB: apply 1 item → set=partially_applied, appearsInQueue=true, reviewableItems=1. No DB-integration test harness exists for the queue, so no automated test added.

**Bug fix — a day-unspecified manual schedule edit only updated ONE row.** "Update worker to Johanna" (no day named) on a client with 3 standing schedules updated only one. Root cause in `client-registry-proposal-drafter.ts`: both `addManualScheduleEditItem` (deterministic patch path) and `refineManualScheduleEditItems` (LLM-item path) resolved a SINGLE target via `findManualScheduleTarget`, which returns null when ambiguous → the edit fell through to one arbitrary row (or was dropped). Fix: added `namesNoSpecificDay(rawText, payload, schedules)` (no day in payload AND no weekday word in prompt) and, when true + `schedules.length>1`, fan out one `operation:"update"` item per standing entry (each with its own `targetId` + per-entry `dayOfWeek` via `inheritSchedulePayload`). Both paths use `.flatMap`/spread. Note the LLM maps `worker_change`→kind `schedule_change`→`operation:"add"` (see `proposalOperationForRegistryEvent`), so the refine guard's `operation!=="add"→skip` is the correct entry condition. The apply path already updates each `clientScheduleEntry` by `item.targetId`, so N items → N rows. Verified: "the worker is Julia" + mon/tue → 2 update items. Test renamed in `client-registry-proposal-drafter.test.ts` ("fans a day-unspecified worker change out to every standing schedule"); the old "does not synthesize…ambiguous" expectation (`[]`) was the bug, now inverted.

**`test/setup.ts` now forces deterministic provider for ALL tests** — sets `MODEL_PROVIDER=mock`+`REGISTRY_LLM_PROVIDER=mock` and `delete`s `GEMINI_API_KEY`/`OPENAI_API_KEY` (overriding, not `??=`, since the dev shell exports a real gemini key). THIS FIXED the long-standing locally-flaky tests (the drafter address-update + extractor "deterministic fallback" cases that previously hit live Gemini). Full suite now 449/449 green locally too, not just CI.

**Bug fix — accepting one suggested update made the whole card vanish; see also the proposal-set status filter widening** ("draft" → ["draft","partially_applied"]) in query-service + analysis-identify-service + roster-service.

**Code-review (max) outcome on the above batch:** 3 fixes applied — (a) `removeRescheduleSupersededCancellations` charge guard narrowed to only `chargePolicy==="late_fee"` (real values are only "charge"/"no_charge" via `inferChargePolicy`; a `no_charge` superseded cancel SHOULD drop since the reschedule subsumes the moved slot); (b) worker-reject regex now allows hyphen: `care[\s-]*worker`/`support[\s-]*worker`; (c) removed dead `ORIGINAL_ENV`/`beforeAll` scaffolding from test/setup.ts. Cross-file + apply-path traces found the fan-out → apply (per-targetId update) and partially_applied filter consistent; no further bugs.

**NEW "Updates" screen replaced the Analysis page (2026-05-31).** A thread-centric worklist at `/updates` (top-level nav "Updates", `UpdatesPage.tsx`+`UpdatesPageSections.tsx`) replaced the old 4-tab `RosterAnalysisPage` (DELETED; `/clients/analysis`+`/clients/reviews` now redirect to `/updates`). It lists ALL snapshot threads (~2129) email-by-email, each carrying its lifecycle inline: identify → proposals → done(greyed). Backend `src/services/roster-worklist-service.ts` `getRosterWorklist` (route `GET /api/roster/updates`) is thread-driven + paginated (50/page), with a pure `deriveThreadStatus` (needs_identify→invoices→ignored→needs_review→no_signal→done) and lazy candidate-ranking (only on pages with a needs_identify thread). Reuses the identify ranking via extracted `buildIdentifyReviewItem`/`toIdentifyCandidatePool` in `analysis-service-helpers.ts` (getIdentifyQueue refactored onto them, candidate parity verified 0 mismatches). Frontend reuses `IdentifyTabContent`+`ReviewCard`+`InvoiceRowActions` via a new `useIdentifyCardController` hook (shared providers/clients fetch across cards). Filters: To do(=identify+review)/Identify/Review/Done/Ignored/Invoices/All. The **Update clients** reprocess button MOVED here off the Clients page (now pure view/edit). Async resolve→proposal bridge re-polls the page on `[1,3,6,10,15]s` with a "Drafting…" interim. Verified: 468 tests green, both tsc clean, client build clean, smoke vs real snapshot (all=2129, needs_action=89). NOT yet visually verified in an authenticated browser. Plan: `~/.claude/plans/lets-look-at-the-noble-charm.md`.

**Prod operator-debugging session (2026-05-31) — 7 fixes shipped + verified live.** Acted as the ops user in prod (Updates screen), applied 1 real update, found 9 issues, fixed 7, pushed (CI deploy), then `roster:reset-suggestions` + bounded `roster:reprocess -- --limit 50` via `with-gcp-db.sh` → verified CLEAN (pastDatedOneOffs=0, unresolvedProvider 38→5). Fixes: (#1) `/updates` was 404 on deep-link/refresh — server SPA allowlist `protectedSpaRoutes` in `src/routes/frontend.ts` was missing it (Vite masks this locally — always add new client routes there too). (#8 dominant) reprocessing a long thread re-extracts full history → past-dated retrospective proposals; new `dropStalePastOneOffServiceEvents` (postprocess) applied in `normalizeExtractedRegistryItems` (NOT the shared `normalizeAndFilterOperationalEvents` — that broke 32 date-fixture tests; it must stay pure) drops past-dated (>7d) non-billing one-off SERVICE points, keeps billing/standing/recent/future. (#7) "invoice"-subject emails that signal a service change (cancel/do-not/reschedule) no longer auto-file as Invoice — `INVOICE_SUBJECT_SERVICE_OVERRIDE` in `roster-thread-classifier.ts`; FORWARD-LOOKING (existing misclassified threads stay until a new message/reset). (#2) auto-ignore vendor noise: added spotify.com/originenergy.com.au/xref.com domains + privacy-policy/budget/supplier-portal subject terms. (#6) ✗-ignore no longer flashes "Drafting…". (#4) ✗-ignore now passes reason `not_a_client_update` → lands in Ignored not Done. (#9) `normalizeOneOffMoveToReschedule` re-maps a one-off "moved 13th→14th" to reschedule — LIMITATION: day-first dates only (`extractDateAfterKeyword` regex is day-first; month-first "May 13th" not handled). Prod URL: https://email-ops-summary-acxbd3ruka-ts.a.run.app (service email-ops-summary, australia-southeast1).

**Settings "Clear update history" (`resetRosterData`) now also deletes `clientRegistrySource where sourceType="gmail_thread"`** (scoped to mailbox via `sourceRef`=gmailThreadId), matching `scripts/reset-suggestions.ts` so a reprocess starts truly clean (no orphaned sources). FKs into the source are all `onDelete:SetNull`. GOTCHA: clearing does NOT reprocess — you must then run a **FULL** reprocess (`?full=true` / `roster:reprocess`); delta mode regenerates nothing since no threads "changed".

**Known flaky tests (cross-file vitest pollution, NOT regressions — each passes in isolation):** `client-registry-proposal-drafter.test.ts` "adds a deterministic Gmail address update…" + "does not synthesize a worker update when ambiguous"; and `client-registry-llm-extractor.test.ts` "deterministic fallback" fails LOCALLY only because the shell exports real Gemini (`.env.local`) so the LLM is available when the test expects it unavailable — passes in CI/mock. Run a single test file alone to confirm green.
